name: Repo Aquarium

on:
  schedule:
    - cron: "17 3 * * *"
  workflow_dispatch:

permissions:
  contents: write
  actions: read

concurrency:
  group: repo-aquarium
  cancel-in-progress: false

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: JongHyun070105/repo-aquarium@b0880ef25e74aaa7fed310f05caf4a1f731b0bbb # v1
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          themes: coral-day,deep-ocean,github-dark,sunset-lagoon,arctic-ice,neon-cyber
          creatures: contributors,jellyfish,crab,turtle,seahorse,octopus,ray,pufferfish,starfish
          ci-workflow: cicd.yml
